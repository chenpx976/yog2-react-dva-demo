{
  "name": "home",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "mockjs": "^1.0.1-beta3",
    "qs": "^6.2.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "yog2 release debug --watch --live",
    "dev": "yog2 release debug --clean --watch --live",
    "pull": "git pull --rebase",
    "push": "git push origin master:refs/for/master",
    "sandbox": "yog2 release debug-prod --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
